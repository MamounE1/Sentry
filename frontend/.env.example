# Backend API base URL.
# Local dev: leave this unset — the Vite dev server proxies /api to http://localhost:8080.
# Production: set to your deployed backend origin (no trailing slash), e.g.
#   VITE_API_URL=https://sentry-backend.onrender.com
# VITE_API_URL=
